qutebrowser overlay
-------------------

A dedicated overlay to install qutebrowser on Gentoo. Add the overlay
with Layman:

    # layman -a qutebrowser

Afterwards, install qutebrowser:

    # emerge -av qutebrowser

